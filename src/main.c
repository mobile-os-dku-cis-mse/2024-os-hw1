#include <stdio.h>

#include "shell.h"

int main(int arg_num, char* args[]) {
    printf("[preflight] check SiSH argument\n");

    // maybe some operation but....yeah no time for the job
    run_shell();
}
